export class Board {
  public id: string;
  public height: string;
  public width: string;

  public constructor(id: string, height: string, width: string) {
    this.id = id;
    this.height = height;
    this.width = width;
  }
}
